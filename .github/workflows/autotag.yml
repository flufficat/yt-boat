on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: '0'
    - name: Bump version and push tag
      uses: anothrNick/github-tag-action@1.39.0
      env:
        GITHUB_TOKEN: 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
        WITH_V: true
        DEFAULT_BUMP: patch
        VERBOSE: true
